# Colab requirements for the Day 1 bootstrap.
#
# Binary and platform packages supplied by Colab are intentionally omitted:
# torch, torchvision, numpy, h5py, scipy, pandas, Pillow, OpenCV, scikit-image,
# matplotlib, imageio, TensorBoard, protobuf, and the Hugging Face stack.
# Replacing those packages in a live kernel can create NumPy ABI conflicts or
# break Colab services. Their exact runtime versions are recorded in evidence.
# Upstream's environment.yml remains unchanged for non-Colab environments.

# Packages missing from the Colab runtime that NanoWM imports during the
# Day 1 configuration smoke test.
# Colab's IPython declares Jedi but does not currently preinstall it; include
# the Python 3.12-compatible release so `pip check` can remain a strict gate.
jedi==0.19.2
hydra-core==1.3.2
pytorch-lightning==1.9.5
torchmetrics==1.2.0
lpips==0.1.4
piqa==1.3.2
decord==0.6.0
pytorch-fid==0.3.0

# Day 1 VizDoom collection
vizdoom==1.3.0
